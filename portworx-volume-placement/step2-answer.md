The solution file with the required volume placement strategies is placed at /tmp/statefulset-config.yaml. To create it run the below command:

`kubectl apply -f /tmp/statefulset-config.yaml`{{execute}}
